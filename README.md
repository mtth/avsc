# Avsc

## API

### Fragments

```javascript
var Record = avsc.parse({});
var record = new Record(f1, f2, f3);

Record.decode(buf);
record.encode();

Record._decode(obuf);
Record._encode(record, obuf);
```

### Object container files

TODO
