{
  "name": "@avro/__root__",
  "private": true,
  "scripts": {
    "clean": "rm -rf .nyc_output node_modules packages/.coverage",
    "uploadCoverage": "./scripts/merge-coverage-reports packages/.coverage/* | coveralls ."
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "nyc": "^15.1.0"
  }
}
