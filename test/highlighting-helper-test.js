'use strict';
var helper = require('../js/lib/highlighting-helper'), 
    assert = require('assert'),
    avsc = require('avsc'),
    buffer = require('buffer'),
    path = require('path');

suite('parsing-schema', function() {
  test('something useful', function() {
    assert.equal(23,23);
  });
});


